[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/trpc/search.programs"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
